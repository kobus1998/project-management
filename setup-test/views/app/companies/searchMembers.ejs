<div style="flex: 1">

  <% include ../../partials/companySidebar %>

  <div>
    <div class="notification is-info">
      <h3 class="title h3">Search members</h3>
    </div>

    <div class="form-content">
      <form method="get">
        <div class="field has-addons">
          <p class="control full-width">
            <input type="search" name="s" class="input" placeholder="search member"
            <% if (typeof req.allParams()['s'] !== 'undefined' && req.allParams()['s'].length > 0) { %>
              value="<%= req.allParams()['s'] %>"
            <% } %>>
          </p>
          <p class="control">
            <button type="submit" class="button is-light">
              <i class="fa fa-search"></i>
            </button>
          </p>
        </div>
      </form>

      <% if (typeof req.allParams()['s'] !== 'undefined' && req.allParams()['s'].length > 0) { %>
        <ul class="list paddingTop">

          <% search[0].members.forEach(function(member) { %>
            <li class="list-item"><%= member.email %></li>
          <% }) %>

        </ul>
      <% } %>



    </div>

  </div>

</div>
